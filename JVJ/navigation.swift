import Foundation
import SwiftUI
/*
struct Navigation: View {
    
    var body: some View {
        
        TabView{
            HomeScreen()
                .tabItem(){
                    Image(systemName: "house")
                    Text("Home")
                }
            
            ARTest(person: Person.samplePerson)
                .tabItem(){
                    Image(systemName: "gear")
                    Text("AR")
                }
        }
        //.accentColor(Color.purple)
        .onAppear() {
            UITabBar.appearance().backgroundColor = UIColor(Color(red: 248 / 255, green: 223 / 255, blue: 253 / 255))
            UITabBar.appearance().unselectedItemTintColor = UIColor.white
        }
    }
        
    /*Think to look up*/
    /*
     extension UITabBarController {
     override open func viewDidAppear(_ animated: Bool) {
     super.viewDidAppear(animated)
     let standardAppearance = UITabBarAppearance()
     
     standardAppearance.stackedLayoutAppearance.focused.titleTextAttributes = [.foregroundColor: UIColor.white]
     standardAppearance.stackedLayoutAppearance.normal.titleTextAttributes = [.foregroundColor: UIColor.white]
     standardAppearance.stackedLayoutAppearance.selected.titleTextAttributes = [.foregroundColor: UIColor.white]
     
     tabBar.standardAppearance = standardAppearance
     }
     }
     */
    
    struct Navigation_Previews: PreviewProvider {
        static var previews: some View {
            Navigation()
        }
    }
}
*/
